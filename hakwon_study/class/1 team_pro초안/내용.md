### 일기 기록 AI 분석 감정기록

diary.html
 ↓ POST
app.py (/diary)
 ↓
EmotionAnalyzer.analyze()
 ↓
DB 저장 (diaries)
 ↓
result.html

“BMI 계산 웹 예제를 기반으로 구조를 확장하여,
사용자의 일기를 분석해 감정 상태를 도출하는 MVP 웹 애플리케이션을 구현했습니다.”

=================================================================================
Model (M) — “일기분석, DB 담당”
emotion.py, db.py
***모델은 일을 하는애. 컨트롤러가 시키는걸 작업하는역할. crud 같은것.

View (v) 화면담당
templates/diary.html, result.html

Controller 중간관리자
app.py

사용자 -> 컨트롤러 -> 모델 -> 뷰





MVC를 리모컨 비유 말고, 회사 비유로 다시 설명할게
👀 View (뷰) = 안내 데스크

화면 보여줌

입력 받음

일은 안 함

예: diary.html, result.html

🎮 Controller (컨트롤러) = 팀장

“이 버튼 눌렀어? 그럼 저쪽에 일 시켜”

흐름 제어만 함

예: @app.route("/diary") 있는 함수

🛠 Model (모델) = 실무 직원 ⭐

👉 실제로 계산하고, 저장하고, 판단하는 역할


=====================================================================================

/
├── app.py               ← Controller (C) # 메인 Flask 앱 (중앙 허브)
├── emotion.py            ← Model (M) # 감정 분석 비즈니스 로직 ⭐
├── db.py                ← Model (M) # DB 연결 (이미 있음)
│
├── templates/
│   ├── login.html
│   ├── register.html
│   ├── diary.html         ← View (V) # 일기 입력
│   ├── result.html      ← View (V) # 감정 결과
│
└── static/
    └── style.css

=================================================

기획서 -> 다빈, 상훈
코딩
유지보수 - > 
기능 추가
 1. 감정 원인 패턴파악
 2. 감정에 맞는 좋은 글귀 / 책 추천